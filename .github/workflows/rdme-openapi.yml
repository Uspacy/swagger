# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2024-08-20T19:16:57.511Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command on api.yml 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi schemas/api.yml --key=${{ secrets.README_API_KEY }} --id=652420c1e726c6001363e803

      - name: Run `openapi` command on commentsService.yml 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi schemas/commentsService.yml --key=${{ secrets.README_API_KEY }} --id=6868f4b1da6852001220494e

      - name: Run `openapi` command on NewsFeedService.yml 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi schemas/newsFeedService.yml --key=${{ secrets.README_API_KEY }} --id=6868f6e41185bd006374df2f

      - name: Run `openapi` command on messenger.yml 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi schemas/messenger.yml --key=${{ secrets.README_API_KEY }} --id=65bb75e4c7253000782471b1

      - name: Run `openapi` command on activitiesService.yml 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi schemas/activitiesService.yml --key=${{ secrets.README_API_KEY }} --id=697f0ab2d52da0dac0439132

      - name: Run `openapi` command on crmService.yml 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi schemas/crmService.yml --key=${{ secrets.README_API_KEY }} --id=69916c1ca5a214942bf2eb38
